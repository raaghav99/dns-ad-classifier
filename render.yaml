services:
  - type: web
    name: dns-ad-classifier
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 60
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
